# TODO

- Stabilize container startup (ports/URL alignment, DB wait, remove network dependency on npx).
- Unify visual style (shared brand header, consistent glass surfaces, shared theme across public/admin/dashboard).
- Fix small UI issues (encoding glitches, extra paddings) without a major redesign.
